# Database — optional in dev (app falls back to in-memory mock data)
DATABASE_URL="postgresql://user:password@localhost:5432/contractor_leads?schema=public"

# Twilio — required for webhook signature validation in production
TWILIO_AUTH_TOKEN=""

# Node environment
NODE_ENV="development"
